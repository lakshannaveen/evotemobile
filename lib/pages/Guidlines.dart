import 'package:flutter/material.dart';

class GuidelinePage extends StatelessWidget {
  const GuidelinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Welcome to Guideline Page'),
      ),
    );
  }
}
